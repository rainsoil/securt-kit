# 内置SQL注入模式规则文件
# 以#开头的行为注释，会被忽略

# 联合查询模式
union select
union all select
union distinct select

# 注释模式
--
/*
*/

# 布尔盲注模式
and 1=1
or 1=1
and 1=2
or 1=2
and true
or true
and false
or false

# 时间延迟模式
waitfor delay
sleep(
benchmark(
pg_sleep(

# 错误注入模式
extractvalue(
updatexml(
exp(
pow(

# 子查询模式
(select
(select *
(select count
(select 1
(select null

# 存储过程模式
sp_
xp_
ms_

# 系统函数模式
@@version
@@user
@@database
@@hostname
@@servername
@@language
@@connections
@@max_connections
@@pack_received
@@pack_sent
@@packet_errors
@@total_read
@@total_write
@@uptime
